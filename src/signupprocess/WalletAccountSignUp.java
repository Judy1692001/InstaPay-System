package signupprocess;

public class WalletAccountSignUp extends SignUp{

    private User [] walletDB;

    @Override
    boolean authenticateUser (User u)
    {
        //must add user to instapaydb after signing up successfully
    }
}
